import { NgFormRefDirective } from './ng-form-ref.directive';

describe('NgFormRefDirective', () => {
  it('should create an instance', () => {
    const directive = new NgFormRefDirective();
    expect(directive).toBeTruthy();
  });
});
